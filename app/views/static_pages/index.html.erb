<div class="jumbotrom">
  <h1> Contac List </h1>
  <p>
    Gerencia sues contatos em qualquer lugar!
  </p>

  <p>
   <% if user_signed_in? %>

      <%= link_to 'Meus Contatos', contacts_path, class: 'btn btn-success btn-lg' %>

    <% else %>

      <%= link_to 'Crie sua Conta', new_user_path, class: 'btn btn-primary btn-lg' %>

    <% end %>
  </p>
</div>
